[package]
name = "mcp-runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
desktop-runtime-core = { path = "../desktop-runtime-core" }
mcp-core = { path = "../mcp-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
time = { version = "0.3", features = ["formatting", "macros", "parsing"] }
