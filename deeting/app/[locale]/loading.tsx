import { StartupShell } from "@/components/common/startup-shell"

export default function Loading() {
  return (
    <StartupShell
      label="Loading workspace"
      detail="Preparing the current route and desktop shell"
    />
  )
}
