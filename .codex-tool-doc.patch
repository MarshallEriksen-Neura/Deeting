*** Begin Patch
*** Add File: docs/tool-architecture.md
+# Deeting 工具封装架构（Tool Surface, Capability Registry, Execution Routing）
+
+> 适用范围：Deeting 当前系统里“模型能看到什么工具、这些工具如何被封装、如何被发现、如何执行、如何审批、如何和 skill / MCP / 本地执行拼起来”。
+> 不覆盖：完整 agent DAG 恢复与挂起机制（见 [agent-dag-architecture.md](./agent-dag-architecture.md)）、RAG / context 编排（见 [rag-architecture.md](./rag-architecture.md)）、长期记忆（见 [memory-architecture.md](./memory-architecture.md)）。
+
+这篇文档不是在讲“工具调用”这个抽象概念，而是在回答一个更实际的问题：Deeting 现在到底把哪些东西当成工具，它们分别封装在哪一层，模型最终又是通过什么边界调用它们的。
+
+如果你要复盘、理解代码、或者准备新增一种 capability，这篇文档应该比直接 grep `tool_call` 更省时间。
+
+## 1. TL;DR
+
+Deeting 当前的工具面不是单层设计，而是 5 层叠在一起：
+
+```text
+Model-visible tool surface
+        |
+        v
+tool_catalog.rs
+  - core tools
+  - lane aux tools
+  - dynamic direct capability tools
+        |
+        v
+capability registry / search_sdk
+  - MCP tools
+  - skill tools
+  - local assistants
+  - recipes / orchestration primitives
+        |
+        v
+tool_execution.rs
+  - resolve route
+  - policy + approval
+  - execute local skill / MCP / shell / sandbox / core execution
+        |
+        v
+runtime / store / UI projection
+```
+
+当前系统里真正需要区分的不是“工具有多少个”，而是 4 类工具来源：
+
+1. Core tools：桌面 runtime 内建的模型可调用工具，由 Rust 直接暴露。
+2. Dynamic direct capability tools：从 capability registry 动态长出来、但仍然作为 function tool 暴露给模型的能力。
+3. Skill tools：来自 `packages/official-skills/` 或本地安装 skill 的 host-callable 工具，通常带 `SKILL.md + deeting.json + llm-tool.yaml + main.py`。
+4. MCP / remote tools：通过 MCP server、stdio / SSE transport 或 host bridge 执行的外部能力。
+
+额外还有两类不是“业务能力工具”但对运行时很重要：
+
+- orchestration primitives：例如 `search_sdk`、`attach_capability`、`detach_capability`，它们更像能力控制面。
+- execution backends：例如 `shell_execute`、本地 sandbox / code execution，它们是执行宿主，不是知识型 capability。
+
+## 2. 为什么这个系统要把“工具”拆成多层
+
+如果只做一个简单聊天助手，工具一般就是一组固定 JSON schema。但 Deeting 不是这个模型。
+
+当前仓库里，工具要同时满足几件事：
+
+- 模型需要稳定、provider-safe 的 function schema，否则 OpenAI-compatible provider 会因为名字非法拒绝调用。
+- 桌面 runtime 需要动态看见用户已安装/已启用的能力，不能把所有 skill 和 MCP tool 都静态写死。
+- 同一个能力要区分“可发现”和“可直接调用”。有些东西只能先通过 `search_sdk` 发现，再 attach 或 delegate；不是每个 registry entry 都应该直接暴露成 function tool。
+- 执行侧需要统一风险和审批。`shell_execute`、浏览器动作、stdio MCP tool、local skill host tool，最后都要过同一层 policy / approval gate。
+- 前端要能解释来源。skills 页面、MCP 页面、capability 设置、tool trace 卡片，不只是展示一个名字，而是要知道它来自 core / skill / MCP / assistant 的哪一层。
+
+所以 Deeting 现在的设计，本质上是把“工具”拆成三个问题：
+
+1. 模型当前看见什么。
+2. 系统总共知道什么 capability。
+3. 实际执行时该走哪条 route。
+
+这三个问题分别落在不同文件里。
+
+## 3. 第一层：模型可见工具面
+
+模型每一轮真正能调用哪些 function tool，入口在 [`deeting/src-tauri/src/modules/desktop_runtime/runtime/tool_catalog.rs`](../deeting/src-tauri/src/modules/desktop_runtime/runtime/tool_catalog.rs)。
+
+核心函数是：
+
+- `build_local_runtime_tools_with_allowlist(...)`
+- `resolve_provider_tool_name_for_execution(...)`
+
+`build_local_runtime_tools_with_allowlist(...)` 先拿一份 `allowed_tool_names`，然后只暴露 allowlist 内的工具，而不是把系统里所有 capability 一股脑扔给模型。
+
+`tool_catalog.rs` 当前把模型可见工具拼成三段：
+
+1. `build_core_tool_function_entries()` 返回的 core tools。
+2. `build_local_execution_lane_aux_tools()` 返回的 lane aux tools。
+3. `build_dynamic_direct_capability_tools(...)` 返回的 dynamic direct capability tools。
+
+其中 lane aux tools 目前至少包括 `attach_capability` 和 `detach_capability`。
+
+同时这一层还明确区分 canonical name 和 provider-safe tool name。`provider_safe_tool_name_for_callable(...)`、`alias_tool_definition_for_provider(...)`、`resolve_provider_tool_name_for_execution(...)` 共同保证：模型看到的名字可以被 alias，但执行层仍能映射回真实 canonical name。
+
+## 4. 第二层：capability registry 不是 tool list，而是能力总表
+
+如果说 `tool_catalog.rs` 决定“这一轮给模型哪些工具”，那么 capability 总表的核心是 [`deeting/src-tauri/src/modules/desktop_runtime/runtime/capability_discovery.rs`](../deeting/src-tauri/src/modules/desktop_runtime/runtime/capability_discovery.rs)。
+
+这一层的中心不是 function tool，而是 `search_sdk` 返回的 capability search result。关键函数包括：
+
+- `build_capability_search_result(...)`
+- `build_capability_search_result_bundle_with_feedback(...)`
+
+从代码上看，registry 结果并不只包含“可直接执行的 function tool”。它还会整理 callable capabilities、recipes、orchestration primitives、delegation targets、local assistants / custom task agents。
+
+也就是说，registry 的角色更像“能力控制平面”，而不是“OpenAI tools 数组”。此外，`capability_discovery.rs` 还融合了 lexical / structured / semantic retrieval、reciprocal rank fusion、feedback affinity 和 query profile 推断，所以它本质上已经是“根据 query 排序能力候选”，而不是简单列清单。
+
+## 5. 第三层：skill 封装是 docs-first + callable contract 的双轨结构
+
+当前仓库里，skill / plugin 的真实封装规范已经很清楚，最直接的入口是：
+
+- [`packages/README.md`](../packages/README.md)
+- [`deeting/src-tauri/src/modules/skills/registry_impl.rs`](../deeting/src-tauri/src/modules/skills/registry_impl.rs)
+- `packages/official-skills/*`
+
+从 `packages/README.md` 的规范看，一个标准 Deeting skill 至少围绕这几样东西：`SKILL.md`、`deeting.json`、`llm-tool.yaml`、`main.py`、`ui/`。
+
+这套结构说明：`SKILL.md` 不是 callable schema；`llm-tool.yaml` 才是 host 可注册的工具合同；`deeting.json` 管运行时与打包元信息；`main.py` 才是真执行体。
+
+以 [`packages/official-skills/crawler/llm-tool.yaml`](../packages/official-skills/crawler/llm-tool.yaml) 为例，一个 skill package 可以导出多个 callable tools，例如 `fetch_web_content` 与 `crawl_website`。因此 skill package 和 tool 并不是 1:1。
+
+`skills/registry_impl.rs` 则负责把磁盘 bundle 变成运行时能力，包括规范 skill id、识别本地安装 skill、解析 manifest 与 runtime 配置、处理冲突迁移，以及把安装状态写进本地 store / capability registry。
+
+## 6. 第四层：统一执行层把 core / skill / MCP / shell 都拉到同一个审批边界
+
+真正把调用落地的关键文件，是 [`deeting/src-tauri/src/modules/mcp/commands/runtime/tool_execution.rs`](../deeting/src-tauri/src/modules/mcp/commands/runtime/tool_execution.rs)。
+
+这一层至少统一调度了：
+
+- 本地 / 远程 MCP tool：`call_local_stdio_tool(...)`、`call_remote_sse_tool(...)`
+- local skill runtime：`execute_local_mcp_tool(...)`、`execute_skill_binding(...)`
+- execution core tool：`ShellExecuteCoreTool`、`ExecutionRequest`
+- host env / binding resolution：`resolve_skill_binding_by_ref(...)`、`resolve_local_tool_env(...)`
+
+同时它也统一承接了 policy / approval：`assess_policy_risk(...)`、`resolve_approval_decision(...)`、`ApprovalPolicyLevel`、persisted approval rule / session grant。
+
+其中 `shell_execute` 最好单独看作 execution backend tool，而不是普通 skill tool。
+
+## 7. 第五层：前端看到的是 capability / registry 视角，不是裸 tools 数组
+
+从仓库结构上也能看出，前端并不是直接绑定某个 `tools: []` JSON：
+
+- `deeting/components/mcp/*`
+- `deeting/lib/api/mcp*.ts`
+- `deeting/lib/api/skills.ts`
+- `deeting/lib/ai/capability-settings.ts`
+- `deeting/store/capability-settings-store.ts`
+
+这说明前端管理的是 capability、server、source、binding、settings 这些实体，而不是单纯 function tool schema。
+
+## 8. 当前系统里“封装的工具”怎么分类
+
+- Core tools：Rust runtime 直接暴露，包含 `search_sdk`、`shell_execute` 等。
+- Lane aux tools：`attach_capability`、`detach_capability` 这类请求级控制工具。
+- Dynamic direct capability tools：从 capability snapshot 动态生成的直接工具面。
+- Skill tools：由 `SKILL.md + deeting.json + llm-tool.yaml + main.py` 打包的可安装能力。
+- MCP tools：来自 stdio / SSE / remote MCP server 的外部能力。
+- Execution backend tools：本地 shell / sandbox 这类高副作用执行宿主。
+
+## 9. 一条真实调用链
+
+```text
+user request
+  -> model gets allowlisted tools from tool_catalog
+  -> model may call search_sdk
+  -> capability_discovery ranks registry entries
+  -> runtime decides direct tool / attached capability / delegated path
+  -> tool_execution resolves route
+  -> approval policy runs if needed
+  -> core tool / skill binding / MCP server / shell backend executes
+  -> result returns to runtime and chat trace
+```
+
+最容易搞混的地方有三个：
+
+1. `search_sdk` 是工具，但它返回的是 capability control-plane 结果，不等于立刻执行。
+2. skill package 和 tool name 不是 1:1，一个 skill 可导出多个 callable tools。
+3. 模型看到的 provider-safe 名字，不一定是执行层的 canonical name。
+
+## 10. 新增一种工具时，应该落在哪一层
+
+- 如果它是系统内建、稳定存在、需要强宿主控制，放进 core tools。
+- 如果它本质是一个可安装、可发布、可复用的功能包，做成 skill package。
+- 如果它来自外部系统或需要独立服务宿主，优先走 MCP tool。
+- 如果它更适合先被发现，而不是一上来直接给模型调用，先进入 capability registry / `search_sdk`，不必直接暴露成 function tool。
+
+## 11. 最值得读的文件
+
+- [`deeting/src-tauri/src/modules/desktop_runtime/runtime/tool_catalog.rs`](../deeting/src-tauri/src/modules/desktop_runtime/runtime/tool_catalog.rs)
+- [`deeting/src-tauri/src/modules/desktop_runtime/runtime/capability_discovery.rs`](../deeting/src-tauri/src/modules/desktop_runtime/runtime/capability_discovery.rs)
+- [`deeting/src-tauri/src/modules/mcp/commands/runtime/tool_execution.rs`](../deeting/src-tauri/src/modules/mcp/commands/runtime/tool_execution.rs)
+- [`deeting/src-tauri/src/modules/skills/registry_impl.rs`](../deeting/src-tauri/src/modules/skills/registry_impl.rs)
+- [`packages/README.md`](../packages/README.md)
+- [`packages/official-skills/crawler/llm-tool.yaml`](../packages/official-skills/crawler/llm-tool.yaml)
+
+## 12. 一句话结论
+
+Deeting 当前的“工具系统”不是一份静态 tool list，而是一个分层结构：tool catalog 决定模型这轮能调什么，capability registry 决定系统总共知道什么，tool execution 决定实际怎么调，skill / MCP / shell 只是不同来源和执行宿主。
*** End Patch
