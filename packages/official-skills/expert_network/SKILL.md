---
name: expert_network
description: "Retrieve expert assistants for a given intent query."
---

# Expert Network

## Overview

Retrieve expert assistants for a given intent query.

- Runtime: `cloud`, `local`

## Available Tools

- `consult_expert_network` — Search expert assistants by intent query and return top candidates. Required: `intent_query`, `confidence`. Optional: `k`.

## Usage Notes

- Keep requests within the tools listed above.
