{
  "name": "create-deeting-plugin",
  "version": "2.0.0",
  "description": "Scaffold a new Deeting OS plugin instantly.",
  "type": "module",
  "main": "./src/index.ts",
  "bin": {
    "create-deeting-plugin": "./src/index.ts"
  },
  "files": [
    "src",
    "templates"
  ],
  "scripts": {
    "dev": "bun ./src/index.ts",
    "prepublishOnly": "rm -rf ./templates && mkdir -p ./templates && cp -r ../templates/default-plugin ./templates/default-plugin"
  },
  "dependencies": {
    "prompts": "^2.4.2",
    "kleur": "^4.1.5",
    "fs-extra": "^11.2.0"
  }
}
