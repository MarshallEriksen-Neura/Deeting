# Optional host tool contract. Keep this in sync with SKILL.md and main.py.
name: hello_deeting
description: "A simple tool to greet the user and show a status dashboard."
parameters:
  type: object
  properties:
    name:
      type: string
      description: "The name of the person to greet."
  required: ["name"]
